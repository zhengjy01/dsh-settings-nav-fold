# dsh-settings-nav-organizer bundle patch: mount the browser plugin row.
# Applied after every bundle layer; the row is inserted into the composition.
- insert:
    - id: settings-nav-fold
      name: dsh-settings-nav-organizer
